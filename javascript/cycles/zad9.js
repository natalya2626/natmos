 "use strict"; 

const start = +prompt("Введите число- начало диапазона: ");
const end = +prompt("Введите число- конец диапазона: ");


if (isNaN(start)) || (isNaN(end)) {
  console.cog ("wrong input");
} else {

for(let i = start; i < end; i++) {
    i % 2 === 0;
}


functions()

}