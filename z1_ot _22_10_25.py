a = (1, 'a', True)   # Создаётся кортеж (tuple) из трёх элементов: целого числа 1,
                     # ❗ Эта переменная больше не используется, поэтому на неё можно не обращать внимания. 
lst = [9, -5, 3]     # создаётся список (list) из трёх целых чисел: 9, -5, 3.
dct = {'a': 1}       # создаётся словарь (dict) с одной парой «ключ-значение»: ключ 'a', значение 1 
                     # ❗ Эта переменная не используется в коде. 

st = {1, 2, 3}       # Создаётся множество (set) из трёх чисел.
                     #  Множество — это неупорядоченная коллекция уникальных элементов.
                     # ❗ Эта переменная тоже не используется. 
for pair in enumerate(lst):  # запускается цикл по результату функции enumerate(lst)
                             # enumerate(lst) преобразует список в последовательность пар (индекс, значение)
# Для lst = [9, -5, 3] это даёт:
# (0, 9)
# (1, -5)
# (2, 3)
# Таким образом, на каждой итерации переменная pair будет содержать кортеж из двух элементов:
#  индекса и значения. 
    print(hash(pair) % 3)  # на каждой итерации: 
                           # Вычисляется хеш объекта pair (а pair — это кортеж, например (0, 9)).
                           # Результат хеширования делится по модулю на 3 (% 3), то есть берётся 
                           # остаток от деления на 3.
                           # Этот остаток (0, 1 или 2) выводится на экран.
                           # 💡 Что такое hash()?
                           # hash() — встроенная функция Python, которая возвращает целое число,
                           #  уникальное (в идеале) для неизменяемого объекта.
                         # Кортежи хешируемы, потому что содержат неизменяемые элементы (int и т. д.).
                         # Хеш одного и того же кортежа всегда одинаков в рамках одного запуска программ

 #  Итог:
# Код проходит по списку [9, -5, 3] с индексами.
# Для каждой пары (индекс, значение) вычисляется хеш, берётся остаток от деления на 3.
# Выводятся три числа: 0, 1 или 2 (в произвольном порядке).
# Переменные a, dct, st — мёртвый код, не влияют на результат.                      
